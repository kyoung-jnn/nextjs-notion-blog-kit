# This file is intentionally minimal.
# All blog configuration is in blog.config.ts
#
# Add environment-specific overrides here if needed
# (e.g., different settings per Vercel preview vs production)
