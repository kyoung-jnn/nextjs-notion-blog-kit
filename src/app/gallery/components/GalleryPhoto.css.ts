import { style } from '@vanilla-extract/css';

export const figure = style({
  all: 'unset',
});

export const photo = style({
  position: 'relative',
  width: '100%',
  height: 600,
  borderRadius: 1,
  overflow: 'hidden',
});
